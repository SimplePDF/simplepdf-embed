// Public /schemas entry: the zod schemas for every operation input, generated
// from embed-api.json. zod is a peer dependency. Each schema's value + inferred
// type share a name (the canonical zod idiom).
export * from './generated/schemas'
