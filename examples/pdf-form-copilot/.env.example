# Server-only. Never commit a real key.
ANTHROPIC_API_KEY=

# Optional: override the in-memory rate limit (per-IP).
# Useful for local development. Defaults are 10 / hour + 50 / day.
# RATE_LIMIT_PER_HOUR=1000
# RATE_LIMIT_PER_DAY=5000
